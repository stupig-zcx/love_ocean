把你和女朋友的本地照片放到这个目录，支持：jpg、jpeg、png、webp、avif、gif。

推荐：
1. 文件名使用英文/数字，例如：story-01.jpg、story-02.jpg。
2. 数量建议与故事条目数一致（当前是 6 条）。

加载规则：
- 如果故事里配置了 `localFile`，优先按文件名精确匹配。
- 否则按文件名排序后，依次分配给每条故事。
- 如果本地图片不足，会自动使用默认网络图兜底。
